# Demo assets

Place the real recording here:

- `demo.mp4` (preferred) or `demo.gif`
- Record:
  - Claude Code or terminal
  - `/speak last` after a response
  - `/speak on` then another query to show autoSpeak
- Keep short (<45s), include sound + visible terminal
- Update README.md to embed the video (remove this placeholder note)

Once recorded:
<video src="demo.mp4" controls style="max-width:100%"></video>
