{
  "description": "OutLoud speaker plugin hooks. Captures last assistant output on Stop (and autoSpeak when enabled) so /speak, hotkey, and status badge work.",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node",
            "args": ["${CLAUDE_PLUGIN_ROOT}/hooks/save-last.js"],
            "timeout": 10
          }
        ]
      }
    ]
  }
}
